{
  "name": "wedding-planner",
  "version": "1.0.0",
  "description": "Wedding Planner Application",
  "scripts": {
    "build": "cd server && npm install && npm run build:clean && cd ../client && npm install && npm run build",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build:clean",
    "build:server:clean": "cd server && npm run build:clean",
    "dev": "cd client && npm run dev",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "install-all": "npm run install-client && npm run install-server",
    "start": "cd server && npm start"
  },
  "keywords": ["wedding", "planner", "react", "node"],
  "author": "Your Name",
  "license": "MIT"
} 